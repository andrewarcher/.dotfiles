# Cloned by `[bootstrap.repos]` in config.toml, not committed here -- these
# used to be git submodules.
/dotfiles/zsh/fzf-tab/
/dotfiles/nvim/pack/plugins/start/nvim-lspconfig/

# mise writes lockfiles next to the config when `lockfile` is enabled; the
# global toolset is intentionally unpinned ("latest").
/mise.lock
