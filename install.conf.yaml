- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    # ~/.tmux.conf: tmux.conf
    # ~/.vim: vim
    ~/.zshrc: target/zshrc
    ~/.vimrc: target/vim/vimrc

- create:
    - ~/.shell_keys

- shell:
  - [git submodule update --init --recursive, Installing submodules]
